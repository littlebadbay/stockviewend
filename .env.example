# Server
PORT=3000
LOG_LEVEL=debug
# Optional WebSocket auth token (leave empty to disable auth)
# WS_AUTH_TOKEN=

# Redis (used by ioredis)
# For docker-compose, the host is the redis service name
REDIS_URL=redis://redis:6379
