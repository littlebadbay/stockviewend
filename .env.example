# Server
PORT=3000
LOG_LEVEL=debug

# CORS
# Comma-separated list of allowed origins. Use * to allow all (development default)
CORS_ALLOWED_ORIGINS=*

# WebSocket toggle
WS_ENABLED=true

# Redis (used by ioredis)
# For docker-compose, the host is the redis service name
REDIS_URL=redis://redis:6379
# For Upstash Redis, use the rediss:// URL with password (TLS required), e.g.:
# REDIS_URL=rediss://:PASSWORD@eu1-glossy-macaw-12345.upstash.io:12345
